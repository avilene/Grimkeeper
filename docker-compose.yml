services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: file:/app/data/grimkeeper.db
    volumes:
      - grimkeeper-data:/app/data

volumes:
  grimkeeper-data:
